export const MOBILE_MAX_WIDTH = 600;

export const APP_MAX_WIDTH = 1000;

export const device = {
  sm: `@media (max-width: ${MOBILE_MAX_WIDTH}px)`,
  lg: `@media (min-width: ${MOBILE_MAX_WIDTH}px)`,
};
